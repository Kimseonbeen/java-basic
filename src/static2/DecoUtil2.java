package static2;

public class DecoUtil2 {


    // 객체로 생성을 해봐야 의미가 없음
    // 멤버(인스턴스)변수도 없고,,
    public static String deco(String str) {
        String result = "*" + str + "*";
        return result;
    }
}
